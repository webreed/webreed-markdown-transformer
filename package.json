{
  "name": "webreed-markdown-transformer",
  "version": "0.0.8",
  "description": "Markdown transformer plugin for webreed (uses markdown-it).",
  "repository": {
    "type": "git",
    "url": "https://github.com/webreed/webreed-markdown-transformer"
  },
  "keywords": [
    "webreed",
    "webreed-plugin",
    "markdown"
  ],
  "main": "lib/setup.js",
  "typings": "lib/setup.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "npm run clean && tsc --pretty",
    "clean": "rm -rf lib",
    "test": "mocha ./test/spec/ --recursive"
  },
  "author": "Rotorz Limited",
  "license": "MIT",
  "dependencies": {
    "markdown-it": "^6.0.0",
    "rxjs": "^5.0.0-beta.2",
    "webreed-core": "^0.0.x"
  },
  "devDependencies": {
    "merge2": "^1.0.1",
    "mocha": "^2.3.4",
    "mocha-testdata": "^1.1.2",
    "should": "^8.0.2",
    "typescript": "^1.8.10"
  }
}
